# SkillSwap City

The frontend application is maintained in [`frontend/`](./frontend). Backend and database work can be added beside it (for example, `backend/` and `database/`) without coupling either team’s code.
